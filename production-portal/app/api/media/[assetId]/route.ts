import { env } from 'cloudflare:workers';
export async function GET(_request:Request,context:{params:Promise<{assetId:string}>}){const{assetId}=await context.params;if(!/^[0-9a-f-]{36}$/i.test(assetId))return new Response('Not found',{status:404});const asset=await env.DB.prepare('SELECT storage_key,mime_type,sha256 FROM assets WHERE id=? LIMIT 1').bind(assetId).first<{storage_key:string;mime_type:string;sha256:string}>();if(!asset?.storage_key)return new Response('Not found',{status:404});const object=await env.FILES.get(asset.storage_key);if(!object)return new Response('Not found',{status:404});const headers=new Headers();object.writeHttpMetadata(headers);headers.set('content-type',asset.mime_type||'image/png');headers.set('cache-control','private, max-age=31536000, immutable');headers.set('etag',object.httpEtag);headers.set('x-content-sha256',asset.sha256);return new Response(object.body,{headers})}
