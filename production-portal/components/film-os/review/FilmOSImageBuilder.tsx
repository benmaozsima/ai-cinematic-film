'use client';
import { useEffect,useMemo,useState } from 'react';
import type { FilmOSShot } from '../types';

export type ImageCorrectionQuote={provider:'Fal';model:string;modelLabel:string;estimatedCost:string};
export type ImageGenerationStatus='submitting'|'queued'|'running'|'completed'|'failed';
export type ImageGenerationRun={runId?:string;jobId?:string;shotId:string;model:string;modelLabel:string;estimatedCost:string;prompt:string;sourceImageUrl:string;requestId?:string;status:ImageGenerationStatus;createdAt:string;updatedAt:string;logs:string[];outputUrl?:string;assetId?:string;assetStatus?:string;version?:string;sha256?:string;width?:number;height?:number;colorMode?:string;error?:string};
const models:Array<ImageCorrectionQuote&{fit:string}>=[
 {provider:'Fal',model:'fal-ai/flux-pro/kontext',modelLabel:'FLUX.1 Kontext Pro',estimatedCost:'$0.04 / image',fit:'ברירת מחדל: עריכת תמונה אחת עם שמירת זהות ופריים.'},
 {provider:'Fal',model:'fal-ai/nano-banana/edit',modelLabel:'Nano Banana Edit',estimatedCost:'$0.039 / image',fit:'חלופת A/B חסכונית; עדיין דורשת QC מלא.'},
 {provider:'Fal',model:'fal-ai/bytedance/seedream/v4.5/edit',modelLabel:'Seedream 4.5 Edit',estimatedCost:'$0.04 / image',fit:'מתאים לעבודה עתידית עם כמה רפרנסים.'},
 {provider:'Fal',model:'fal-ai/flux-pro/kontext/max',modelLabel:'FLUX.1 Kontext Max',estimatedCost:'$0.08 / image',fit:'מסלול הסלמה אחרי כשל QC.'},
];
const labels:Record<ImageGenerationStatus,string>={submitting:'נשלח לתור',queued:'בתור Fal',running:'Fal יוצר כעת',completed:'גרסה חדשה מוכנה לריוויו',failed:'הריצה נכשלה'};

export function FilmOSImageBuilder({shot,note,onBack,onRequestApproval,onOpenImageReview}:{shot:FilmOSShot;note:string;run?:ImageGenerationRun;onBack:()=>void;onRequestApproval:(prompt:string,quote:ImageCorrectionQuote)=>void;onRefreshRun?:()=>void;onOpenImageReview?:()=>void}){
 const[instruction,setInstruction]=useState(note),[modelId,setModelId]=useState(models[0].model),[run,setRun]=useState<ImageGenerationRun>(),[loading,setLoading]=useState(true),[draftSaved,setDraftSaved]=useState(false);
 const quote=models.find(x=>x.model===modelId)??models[0];
 const prompt=useMemo(()=>{const objectConstraint=shot.id==='S003'?'\n\nS003 OBJECT CONSTRAINT — NON-NEGOTIABLE: rotate the entire smartphone 180 degrees around its vertical axis. The camera must see ONLY the physical matte back casing and rear camera lenses. The display must face the actor and be completely invisible to the camera. ZERO display, ZERO call interface, ZERO illuminated screen, ZERO front notch, ZERO buttons or icons may face the viewer. Adjust the hand grip naturally around the phone edges. Reject the result if any screen content is visible.':'';return `Edit only the supplied source keyframe for ${shot.id}. REQUIRED CHANGE: ${instruction}${objectConstraint}\n\nPreserve exactly: CHAR_001 / Shoko A identity, elderly male only, brown cardigan, white shirt, dark hat, cane, hand anatomy, warm window light, vertical 9:16 composition, room and camera framing. Do not add women, girls, children, extra people, duplicate limbs, text, logos, watermark, wardrobe changes, or a mirrored/reversed device.`},[instruction,shot.id]);
 useEffect(()=>{const timer=window.setTimeout(()=>{const saved=window.localStorage.getItem(`production-os:image-draft:${shot.id}`);if(saved){setInstruction(saved);setDraftSaved(true)}},0);return()=>window.clearTimeout(timer)},[shot.id]);
 useEffect(()=>{let active=true;const load=async()=>{try{const response=await fetch(`/api/image-jobs?shotId=${encodeURIComponent(shot.id)}`,{cache:'no-store'}),value=await response.json() as {runs?:ImageGenerationRun[]};let latest=value.runs?.[0];if(response.ok&&latest?.requestId&&['submitting','queued','running'].includes(latest.status)){const sync=await fetch(`/api/image-jobs/${encodeURIComponent(latest.requestId)}?model=${encodeURIComponent(latest.model)}`,{cache:'no-store'}),synced=await sync.json() as {run?:ImageGenerationRun};if(sync.ok&&synced.run)latest=synced.run}if(active&&response.ok)setRun(latest)}finally{if(active)setLoading(false)}};void load();const timer=window.setInterval(()=>void load(),2500);return()=>{active=false;window.clearInterval(timer)}},[shot.id]);
 const active=Boolean(run&&['submitting','queued','running'].includes(run.status));
 const reviewCandidate=Boolean(run?.status==='completed'&&run.outputUrl&&run.assetStatus==='review');
 const rejectedCandidate=Boolean(run?.status==='completed'&&run.assetStatus==='rejected');
 return <section className="fos-image-builder" dir="rtl"><style>{`.fos-image-builder{max-width:1180px;margin:auto}.fos-image-builder header{display:flex;justify-content:space-between;gap:16px;align-items:start;margin-bottom:18px}.fos-image-builder h1{font-size:33px;margin:4px 0}.fos-image-builder p{color:var(--fos-muted);line-height:1.6}.fos-image-builder-grid{display:grid;grid-template-columns:.8fr 1.2fr;gap:17px}.fos-image-builder figure,.fos-image-builder article{margin:0;background:#0d141f;border:1px solid var(--fos-line);border-radius:14px;overflow:hidden}.fos-image-builder img{display:block;width:100%;max-height:630px;object-fit:contain;background:#060a0f}.fos-image-builder figcaption,.fos-image-builder article{padding:16px}.fos-image-builder textarea,.fos-image-builder select{width:100%;box-sizing:border-box;background:#0a111a;border:1px solid var(--fos-line);border-radius:10px;color:#f4f6fb;padding:12px;font:inherit}.fos-image-builder textarea{min-height:145px;line-height:1.6}.fos-image-builder pre{white-space:pre-wrap;overflow:auto;max-height:235px;background:#090f17;border:1px solid var(--fos-line);border-radius:10px;padding:12px;color:#b9c7d9;font-size:12px;line-height:1.55;direction:ltr;text-align:left}.fos-image-quote{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin:14px 0}.fos-image-quote div{padding:10px;background:#111c29;border:1px solid var(--fos-line);border-radius:9px}.fos-image-quote span{display:block;color:var(--fos-muted);font-size:10px}.fos-image-quote b{font-size:12px}.fos-image-run{margin:0 0 14px;padding:13px;border:1px solid #5964a5;background:#151a35;border-radius:11px}.fos-image-run--completed{border-color:#2f956d;background:#10291f}.fos-image-run--failed{border-color:#a75555;background:#351b22}.fos-image-run small{display:block;margin-top:5px;color:#b9c7d9;direction:ltr;text-align:left}.fos-run-progress{height:7px;background:#273246;border-radius:99px;overflow:hidden}.fos-run-progress i{display:block;height:100%;width:45%;background:#8175ff;border-radius:99px;animation:fosmove 1.2s infinite alternate}@keyframes fosmove{to{transform:translateX(120%)}}.fos-image-builder-actions{display:flex;gap:8px;margin-top:14px}.fos-image-builder-actions button{flex:1}@media(max-width:760px){.fos-image-builder-grid{grid-template-columns:1fr}.fos-image-builder header{display:block}.fos-image-quote{grid-template-columns:1fr}}`}</style><header><div><p className="fos-overline">IMAGE CORRECTION PACKAGE · {shot.id}</p><h1>תיקון keyframe לפני וידאו</h1><p>אישור מפורש מתחיל ריצת Fal אמיתית. הריצה נשמרת בשרת וממשיכה גם אם עוברים למסך אחר.</p></div><button className="fos-button" onClick={onBack}>חזרה לריוויו</button></header>{loading&&<section className="fos-image-run">טוען מצב ריצה…</section>}{run&&<section className={`fos-image-run fos-image-run--${run.status}`}><b>{run.shotId} · {rejectedCandidate?'הגרסה האחרונה נדחתה':labels[run.status]}</b>{active&&<><p>אפשר להמשיך לעבוד. הסטטוס מתעדכן אוטומטית כל 2.5 שניות.</p><div className="fos-run-progress"><i/></div></>}{reviewCandidate&&<><p>{run.version} נשמרה ב‑R2 וממתינה לריוויו; המקור לא הוחלף.</p><small>{run.width}×{run.height} · {run.colorMode} · SHA‑256 {run.sha256}</small><button className="fos-button primary" onClick={()=>onOpenImageReview?onOpenImageReview():window.location.assign('#image-review')}>פתח ריוויו לגרסה החדשה</button></>}{rejectedCandidate&&<p>{run.version} נשמרה בהיסטוריה כנדחית ואינה יכולה לשמש כ־first frame.</p>}{run.status==='failed'&&<p role="alert">{run.error}</p>}{run.requestId&&<small>Fal request: {run.requestId}</small>}</section>}<div className="fos-image-builder-grid"><figure><img src={reviewCandidate&&run?.outputUrl?run.outputUrl:shot.thumbnailUrl} alt={`תמונה של ${shot.id}`}/><figcaption>{reviewCandidate?`${run?.version} · מועמדת חדשה לריוויו`:'תמונת מקור · לתיקון · הווידאו חסום עד אישור תמונה'}</figcaption></figure><article><p className="fos-overline">CORRECTION INSTRUCTION</p><h2>הערת התיקון</h2><textarea value={instruction} onChange={e=>{const value=e.target.value;setInstruction(value);window.localStorage.setItem(`production-os:image-draft:${shot.id}`,value);setDraftSaved(true)}}/><small>{draftSaved?'✓ הטיוטה נשמרה במכשיר זה':'הטיוטה תישמר אוטומטית'}</small><label><p className="fos-overline">MODEL</p><select value={modelId} onChange={e=>setModelId(e.target.value)}>{models.map(x=><option key={x.model} value={x.model}>{x.modelLabel} · {x.estimatedCost}</option>)}</select></label><p>{quote.fit}</p><p className="fos-overline">FINAL PROVIDER PROMPT · READ ONLY</p><pre>{prompt}</pre><div className="fos-image-quote"><div><span>ספק</span><b>Fal</b></div><div><span>מודל</span><b>{quote.modelLabel}</b></div><div><span>עלות מרבית</span><b>{quote.estimatedCost}</b></div></div><div className="fos-image-builder-actions"><button className="fos-button" onClick={onBack}>חזרה</button><button className="fos-button primary" disabled={!instruction.trim()||active} onClick={()=>onRequestApproval(prompt,quote)}>{active?'ריצה פעילה…':`בדוק ואשר יצירה · ${quote.estimatedCost}`}</button></div></article></div></section>
}
