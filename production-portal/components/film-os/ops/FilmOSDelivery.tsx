'use client';

import { useMemo } from 'react';
import { FilmOSStatusPill } from '../FilmOSShell';
import type { FilmOSDeliveryProps } from './types';

const time = (seconds: number) => `${String(Math.floor(seconds / 60)).padStart(2, '0')}:${String(seconds % 60).padStart(2, '0')}`;

export function FilmOSDelivery({ shots, selectedVersions = {}, durableMp4Url, durableMp4Label = 'הורדת הסרט המלא', onRequestExportApproval }: FilmOSDeliveryProps) {
  const ordered = useMemo(() => [...shots].sort((a, b) => a.startSeconds - b.startSeconds), [shots]);
  const duration = Math.max(0, ...ordered.map(shot => shot.endSeconds));
  const missingVideo = ordered.filter(shot => !shot.videoUrl);
  const unresolved = ordered.filter(shot => shot.status !== 'approved');
  const clipsReady = ordered.length - missingVideo.length;
  const exportReady = Boolean(durableMp4Url) && !missingVideo.length && !unresolved.length;
  const requestExport = () => onRequestExportApproval?.({
    kind: 'export', selectedShotIds: ordered.map(shot => shot.id),
    notes: [
      `${clipsReady}/${ordered.length} קליפים עם כתובת וידאו אמיתית.`,
      unresolved.length ? `${unresolved.length} שוטים אינם מאושרים עדיין.` : 'כל השוטים מסומנים מאושרים.',
      durableMp4Url ? 'קיים MP4 יציב להורדה.' : 'אין עדיין MP4 מאוגד ב־R2; לא ניתן ליצור קובץ דמה.'
    ]
  });
  return <section className="fos-delivery" aria-labelledby="delivery-title">
    <style>{styles}</style>
    <header className="fos-delivery-head"><div><p className="fos-overline">CUT DELIVERY · SINGLE SOURCE OF TRUTH</p><h2 id="delivery-title">ה־cut שנבחר, לפני שהוא הופך לקובץ.</h2><p>רק קליפים אמיתיים, גרסאות שנבחרו, וסטטוס ריוויו. מסמך HTML לעולם לא יוצע כהורדת סרט.</p></div><span className={`fos-delivery-ready ${exportReady ? 'yes' : 'no'}`}>{exportReady ? 'מוכן למסירה' : 'לא מוכן לייצוא'}</span></header>
    <section className="fos-delivery-overview"><article className="fos-card fos-delivery-master"><div className="fos-delivery-master-copy"><p className="fos-overline">MASTER CUT</p><h3>00:00 — {time(duration)}</h3><p>{ordered.length} שוטים · {clipsReady} קבצי וידאו זמינים · {unresolved.length} החלטות פתוחות</p></div><div className="fos-delivery-actions">{durableMp4Url ? <a href={durableMp4Url} download className="fos-button primary">↓ {durableMp4Label}</a> : <button type="button" className="fos-button" disabled title="נדרש MP4 מאוגד ואחסון יציב לפני הורדה">↓ הורדת MP4 — עדיין לא זמין</button>}<button type="button" className="fos-button" onClick={requestExport}>בדוק מסירה ובקש אישור</button></div></article><article className="fos-card fos-delivery-honesty"><span>מצב אחסון</span><b>{durableMp4Url ? 'MP4 זמין' : 'ממתין ל־R2 / Encoder'}</b><p>{durableMp4Url ? 'הקישור מצביע לקובץ מסירה אמיתי.' : 'הכפתור מושבת בכוונה: אין כאן הורדה מזויפת של אתר או manifest.'}</p></article></section>
    <section className="fos-delivery-checks" aria-label="בדיקות מוכנות"><Check ok={!missingVideo.length} label="לכל שוט יש Video Asset" detail={missingVideo.length ? `חסרים: ${missingVideo.map(shot => shot.id).join(', ')}` : `${clipsReady}/${ordered.length} עם כתובת וידאו`} /><Check ok={!unresolved.length} label="כל השוטים מאושרים" detail={unresolved.length ? `${unresolved.length} פתוחים לריוויו או לתיקון` : 'הרצף נעול למסירה'} /><Check ok={Boolean(durableMp4Url)} label="קיים קובץ MP4 מאוגד" detail={durableMp4Url ? 'ניתן להוריד ולשתף' : 'נדרש worker שמאגד ושומר ל־R2'} /></section>
    <section className="fos-delivery-cut"><div className="fos-delivery-sectionhead"><div><p className="fos-overline">SELECTED ASSEMBLY</p><h3>מה בדיוק ייכנס ל־cut</h3></div><span>{time(duration)} · {ordered.length} שוטים</span></div><div className="fos-delivery-rail">{ordered.map(shot => <article className="fos-delivery-clip" key={shot.id}><div className="fos-delivery-thumb">{shot.thumbnailUrl ? <img src={shot.thumbnailUrl} alt={`${shot.id} — ${shot.title}`}/> : <div>אין Keyframe</div>}<span>{time(shot.startSeconds)}–{time(shot.endSeconds)}</span></div><div className="fos-delivery-clip-copy"><div><b>{shot.id}</b><FilmOSStatusPill status={shot.status}/></div><h4>{shot.title}</h4><p>{selectedVersions[shot.id] || shot.version || 'לא נבחרה גרסה'}</p>{shot.videoUrl ? <a href={shot.videoUrl} target="_blank" rel="noreferrer">פתח קליפ ↗</a> : <span className="fos-delivery-missing">קליפ חסר</span>}</div></article>)}</div></section>
  </section>;
}

function Check({ ok, label, detail }: { ok: boolean; label: string; detail: string }) { return <article className={`fos-card fos-delivery-check ${ok ? 'ok' : 'pending'}`}><b aria-hidden>{ok ? '✓' : '○'}</b><div><h4>{label}</h4><p>{detail}</p></div></article>; }

const styles = `
.fos-delivery{max-width:1400px;margin:auto}.fos-delivery-head{display:flex;align-items:end;justify-content:space-between;gap:20px;margin-bottom:20px}.fos-delivery h2{font-size:clamp(27px,3vw,42px);letter-spacing:-.06em;margin:0}.fos-delivery-head p:not(.fos-overline){max-width:720px;margin:10px 0 0;color:var(--fos-muted);line-height:1.7;font-size:13px}.fos-delivery-ready{white-space:nowrap;font-size:11px;font-weight:850;border-radius:999px;padding:8px 11px}.fos-delivery-ready.yes{color:var(--fos-mint);background:#62ddb315;border:1px solid #62ddb338}.fos-delivery-ready.no{color:var(--fos-warn);background:#f3b96b15;border:1px solid #f3b96b38}.fos-delivery-overview{display:grid;grid-template-columns:1.45fr .55fr;gap:13px}.fos-delivery-master{padding:25px;display:flex;align-items:center;justify-content:space-between;gap:20px;background:radial-gradient(circle at 90% 5%,#7777f236,transparent 34%),linear-gradient(135deg,#192333,#101924)}.fos-delivery-master h3{font-size:28px;margin:5px 0;letter-spacing:-.05em}.fos-delivery-master p{margin:0;color:#acb8c8;font-size:12px}.fos-delivery-actions{display:flex;gap:7px;flex-wrap:wrap;justify-content:flex-end}.fos-delivery-actions button:disabled{cursor:not-allowed;opacity:.52}.fos-delivery-honesty{padding:21px}.fos-delivery-honesty span{display:block;color:#8493a7;font-size:10px}.fos-delivery-honesty b{display:block;margin:7px 0;font-size:15px}.fos-delivery-honesty p{color:#aeb9c8;line-height:1.55;font-size:11px;margin:0}.fos-delivery-checks{display:grid;grid-template-columns:repeat(3,1fr);gap:11px;margin:14px 0 34px}.fos-delivery-check{padding:14px;display:flex;gap:11px}.fos-delivery-check>b{display:grid;place-items:center;width:26px;height:26px;border-radius:50%;font-size:14px}.fos-delivery-check.ok>b{background:#62ddb31c;color:var(--fos-mint)}.fos-delivery-check.pending>b{background:#f3b96b1d;color:var(--fos-warn)}.fos-delivery-check h4{font-size:12px;margin:1px 0 4px}.fos-delivery-check p{font-size:10px;color:#9ba9bb;line-height:1.5;margin:0}.fos-delivery-sectionhead{display:flex;justify-content:space-between;align-items:end;gap:15px;margin-bottom:13px}.fos-delivery-sectionhead h3{font-size:22px;margin:0;letter-spacing:-.045em}.fos-delivery-sectionhead>span{font-size:11px;color:var(--fos-muted)}.fos-delivery-rail{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:11px}.fos-delivery-clip{overflow:hidden;border:1px solid var(--fos-line);border-radius:12px;background:#121a25}.fos-delivery-thumb{height:124px;position:relative;background:#101722}.fos-delivery-thumb img{display:block;width:100%;height:100%;object-fit:cover}.fos-delivery-thumb>div{display:grid;place-items:center;height:100%;font-size:10px;color:#78869a}.fos-delivery-thumb span{position:absolute;left:7px;bottom:7px;background:#0a1118d8;color:#e2e9f5;padding:3px 5px;font-size:9px;border-radius:4px;direction:ltr}.fos-delivery-clip-copy{padding:10px}.fos-delivery-clip-copy>div{display:flex;justify-content:space-between;align-items:center;gap:4px}.fos-delivery-clip-copy>b{font-size:10px}.fos-delivery-clip-copy h4{margin:8px 0 3px;font-size:13px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.fos-delivery-clip-copy p{margin:0;color:#8f9daf;font-size:10px}.fos-delivery-clip-copy a,.fos-delivery-missing{display:inline-block;margin-top:9px;color:#aeb2ff;font-size:10px;text-decoration:none}.fos-delivery-missing{color:#f19f90}@media(max-width:950px){.fos-delivery-overview{grid-template-columns:1fr}.fos-delivery-rail{grid-template-columns:repeat(3,minmax(0,1fr))}}@media(max-width:680px){.fos-delivery-head,.fos-delivery-master{display:block}.fos-delivery-ready{display:inline-block;margin-top:14px}.fos-delivery-actions{justify-content:flex-start;margin-top:18px}.fos-delivery-checks{grid-template-columns:1fr}.fos-delivery-rail{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:420px){.fos-delivery-rail{grid-template-columns:1fr}}
`;
