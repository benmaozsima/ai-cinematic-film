name: Deploy production dashboard

on:
  push:
    branches: [main, work]
    paths:
      - "dashboard/**"
      - "assets/review/**"
      - "assets/characters/**"
      - "assets/environments/**"
      - "assets/keyframes/**"
      - ".github/workflows/deploy-dashboard.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Assemble static site
        run: |
          mkdir -p _site/assets
          cp -R dashboard _site/dashboard
          cp -R assets/review _site/assets/review
          mkdir -p _site/assets/characters _site/assets/environments _site/assets/keyframes
          cp assets/characters/*reference_sheet_v0.2.png _site/assets/characters/
          cp assets/environments/*reference_sheet_v0.2.png _site/assets/environments/
          cp assets/keyframes/S001_real_v0.1.png assets/keyframes/S004_real_v0.1.png _site/assets/keyframes/
          cat > _site/index.html <<'HTML'
          <!doctype html>
          <meta charset="utf-8">
          <meta http-equiv="refresh" content="0; url=./dashboard/">
          <title>The Search Party Production Dashboard</title>
          <a href="./dashboard/">Open production dashboard</a>
          HTML
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
